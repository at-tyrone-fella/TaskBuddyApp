/**
 * Dimensions set on top to be used thoughout the application.
 * Setting it on top will allow any future changes to dimension to be made in this place only.
 */
import { Dimensions } from "react-native";

export const { width, height } = Dimensions.get("window");